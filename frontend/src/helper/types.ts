export interface UserData {
    firstName: string;
    lastName: string;
    username: string;
    email: string;
}
export interface PostData {
    title: string;
    content: string;
    postId: string;
}

export interface TagData {
    tagName: string;
    color: string;
    tagId: string;
}
